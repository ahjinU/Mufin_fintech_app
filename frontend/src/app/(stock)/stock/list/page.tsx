import { BackButton, Header } from '@/components';

export default function StockList() {
  return (
    <div>
      <Header>
        <BackButton label={'내 주식 내역'} />
      </Header>
    </div>
  );
}
